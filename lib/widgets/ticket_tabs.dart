import 'package:flutter/material.dart';

class AppTcketsTabs extends StatelessWidget {
  const AppTcketsTabs({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
